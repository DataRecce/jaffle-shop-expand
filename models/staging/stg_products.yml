models:
  - name: stg_products
    description: Product data with cleaning and transformation applied, one row per product.
    config:
      tags: ['core-dimension']
    columns:
      - name: product_id
        description: The unique key for each product.
        data_tests:
          - not_null
          - unique
